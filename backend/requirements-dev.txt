# Development and test tooling. Not installed in the production image.
-r requirements.txt

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0
faker==22.2.0
# In-process FTP server, so the FTP export is tested against a real server
# rather than a mock. Its tests skip themselves when it is absent.
pyftpdlib==1.5.9

# Code Quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0
